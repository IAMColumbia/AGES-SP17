using UnityEngine;
using System.Collections;

public class EnemyController : MonoBehaviour {
    //Turn the animator on and off, depending on input from the Enemy Manager.
    //Include a spawn timer for when the damn thing is actually supposed to move


	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
